rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Allow all reads and writes during development and testing.
    // WARNING: Do not use these rules in a production environment.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
