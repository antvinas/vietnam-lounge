// apps/web/src/api/ads.api.ts

// 🟢 인자 2개 허용
export const logSponsorViewEvent = async (spotId?: string, type?: string) => {
  if (!spotId) return;
};

export const logSponsorClickEvent = async (spotId?: string, type?: string) => {
  if (!spotId) return;
};